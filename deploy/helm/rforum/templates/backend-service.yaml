apiVersion: v1
kind: Service
metadata:
  name: {{ include "rforum.backend.fullname" . }}
  labels:
    {{- include "rforum.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "rforum.componentSelectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
